#include <jni.h>
/*
 * PigProjectionImpl.cc - unlike other pig objects this projection is not a pig object.
 *                        You will find no PigProjection class under vicar.
 *                        The projection object is held by the ImageBag in the MICA system.
 *                        This object holds info related to projection computations.
 *
 *                        This object is a generalization of computation found (inline) in
 *                        (VICAR_TOP)/mars/src/prog/marsmap.cc
 */
